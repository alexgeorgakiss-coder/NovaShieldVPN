# STEM Robotics — AI Content Studio

Chat-based frontend για παραγωγή SEO άρθρων και Social Media posts.

## Setup

```bash
cp .env.example .env
# Συμπλήρωσε VITE_N8N_WEBHOOK_URL, VITE_AIRTABLE_API_KEY, VITE_AIRTABLE_BASE_ID
npm install
npm run dev
```
